<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>

<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Main MCP package -->
    <logger name="io.modelcontextprotocol" level="INFO"/>

    <!-- Server packages -->
    <logger name="io.modelcontextprotocol.server" level="INFO"/>

    <!-- Client packages -->
    <logger name="io.modelcontextprotocol.client" level="INFO"/>

    <!-- Transport packages -->
    <logger name="io.modelcontextprotocol.client.transport" level="INFO"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
